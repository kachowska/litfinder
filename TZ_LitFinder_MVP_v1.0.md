# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## ИИ-платформа подбора академической литературы "LitFinder"

**Версия:** 1.0  
**Дата:** 22 января 2026  
**Статус:** MVP для разработки  
**Локация:** Беларусь (Минск)  
**Язык:** RU/EN

---

## 📋 СОДЕРЖАНИЕ
1. Введение
2. Целевая аудитория и сценарии
3. Функциональные требования
4. Интеграции (техническая спецификация)
5. Технический стек
6. Нефункциональные требования
7. Структура БД
8. API спецификация
9. Тестирование и приемка
10. Деплой и эксплуатация
11. Риски и план B

---

## 1. ВВЕДЕНИЕ

### 1.1. Цели проекта
Создать автоматизированную ИИ-платформу для поиска релевантных научных статей и генерации списка литературы по ГОСТ для аспирантов, диссертантов и преподавателей СНГ (Беларусь, Россия, Казахстан).

**Основные задачи:**
- ✅ Автоматизировать поиск академических источников (сократить время с 3-5 ч до 15 мин)
- ✅ Генерировать списки литературы по ГОСТ Р 7.0.100-2018 без ошибок
- ✅ Обеспечить легальный доступ к CyberLeninka + OpenAlex
- ✅ Предоставить бесплатную базовую версию с freemium-тарификацией

### 1.2. Ключевые метрики успеха MVP
```
• 300 beta-пользователей за 12 недель
• 10% конверсия в Pro ($5 USD/мес)
• NPS >70
• Break-even через 8 месяцев (300 Pro users)
• Среднее время ответа: ≤2 сек
```

### 1.3. Границы MVP

**✅ В Scope MVP:**
- Семантический поиск по теме (RU/EN)
- Поддержка источников: статьи, книги, диссертации
- Генерация ГОСТ Р 7.0.100-2018 + ВАК
- Telegram-бот + Web-интерфейс
- Freemium (10 запросов/день бесплатно, Pro - 1000/день)
- Экспорт в Word/BibTeX/RIS/Zotero
- Интеграция: CyberLeninka OAI-PMH, OpenAlex API

**❌ Из Scope MVP:**
- eLIBRARY (требует корпоративный договор, будет позже)
- Мобильное приложение (iOS/Android)
- Аналитика цитирований и impact factor
- Машинный перевод статей
- Колаборативные лист (совместный доступ)

---

## 2. ЦЕЛЕВАЯ АУДИТОРИЯ И СЦЕНАРИИ

### 2.1. Целевые персоны

**Персона 1: Аспирант 2-го года**
```
Имя: Елена (25 лет)
Образование: БГУ, специальность "Информационные системы"
Боль: Тратит 3-5 часов на поиск 20 релевантных статей для обзора литературы
Готов платить: 300-500 BYN/год (~6 USD/мес)
Частота использования: 3-4 раза/неделю
```

**Персона 2: Молодой преподаватель вуза**
```
Имя: Иван (32 года)
Образование: БНТУ, кандидат наук
Боль: Подготовка лекций, написание статей требует быстрого подбора актуальных источников
Готов платить: 1000-1500 BYN/год (~15 USD/мес)
Частота использования: 2 раза/неделю
```

**Персона 3: Диссертант (соискатель)**
```
Имя: Виктор (35 лет)
Образование: кандидат наук, готовит диссертацию на степень доктора
Боль: Необходимо систематизировать 500+ источников, следить за новыми публикациями
Готов платить: 2000+ BYN/год (~25 USD/мес)
Частота использования: ежедневно
```

### 2.2. User Stories (MVP)

**Story 1: Поиск по теме (MUST)**
```
Как аспирант, я хочу ввести тему "нейросети в образовании",
чтобы получить 20 релевантных статей за 2 минуты вместо 3 часов ручного поиска.

Acceptance Criteria:
✓ Результаты отсортированы по релевантности (score ≥0.8)
✓ Показаны авторы, год, краткая аннотация
✓ Кнопка "Добавить в список"
✓ Фильтры по году (2020-2025)
```

**Story 2: Генерация списка литературы (MUST)**
```
Как аспирант, я хочу выбрать 15 статей и получить готовый список по ГОСТ,
чтобы вставить его в диссертацию без ошибок оформления.

Acceptance Criteria:
✓ Список в формате ГОСТ Р 7.0.100-2018
✓ Автоматическая нумерация
✓ Проверка ошибок (отсутствие DOI, неполные данные)
✓ Экспорт в Word/PDF
```

**Story 3: Telegram-интеграция (MUST)**
```
Как мобильный пользователь, я хочу находить статьи через Telegram,
чтобы не открывать отдельное приложение.

Acceptance Criteria:
✓ Команды /theme, /list, /export
✓ Инлайн-кнопки для добавления статей
✓ Кэш поиска в чате
```

**Story 4: Экспорт форматов (SHOULD)**
```
Как исследователь Zotero, я хочу экспортировать результаты в BibTeX/RIS,
чтобы синхронизировать с менеджером литературы.

Acceptance Criteria:
✓ Кнопка "Export BibTeX"
✓ Формат совместим с Zotero/Mendeley
```

### 2.3. Market Size & TAM

```
Потенциальный рынок СНГ:
├─ Аспиранты/диссертанты: 160k чел
├─ Преподаватели/научные сотрудники: 630k чел
└─ Общий адресный рынок: 750-900k чел

SAM (10-15% с высоким спросом): 100-150k чел
SOM (год 1): 5-10k DAU, 1-2k Pro users

Рост EdTech СНГ: +19-25% в год
```

---

## 3. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 3.1. Основная функция: Поиск статей

#### 3.1.1. API Endpoint
```
POST /api/v1/search

Request Body:
{
  "query": "нейросети в образовании",
  "limit": 20,
  "offset": 0,
  "filters": {
    "year_from": 2020,
    "year_to": 2025,
    "language": ["ru", "en"],
    "concepts": ["education", "machine-learning"],
    "source": ["cyberleninka", "openalex"]  // опционально
  }
}

Response (200 OK):
{
  "total": 142,
  "results": [
    {
      "id": "openalex_w123456789",
      "source": "openalex",
      "title": "Искусственные нейронные сети в дистанционном образовании",
      "authors": [
        {
          "id": "a123456",
          "name": "Иванов Иван Иванович",
          "initials": "И.И."
        },
        {
          "id": "a123457",
          "name": "Петрова Петра Петровна",
          "initials": "П.П."
        }
      ],
      "year": 2024,
      "journal": "Вестник БГУ. Серия 4",
      "volume": 3,
      "issue": 15,
      "pages": "45-56",
      "doi": "10.1234/abc-def-123",
      "pdf_url": "https://cyberleninka.ru/article/pdf/...",
      "abstract": "В работе рассмотрены основные подходы к применению нейросетей...",
      "abstract_snippet": "применение нейросетей в образовании показывает...",
      "cited_by_count": 12,
      "concepts": ["education", "neural-networks", "AI"],
      "relevance_score": 0.94,
      "open_access": true,
      "keywords": ["нейросети", "образование", "дистанционное обучение"]
    }
  ],
  "filters_applied": {
    "year_from": 2020,
    "year_to": 2025
  },
  "execution_time_ms": 1200
}
```

#### 3.1.2. Параметры поиска

| Параметр | Тип | Обязательный | Пример | Примечание |
|----------|-----|--------------|--------|------------|
| query | string | Да | "нейросети образование" | Минимум 3 символа |
| limit | integer | Нет (дефолт 20) | 20 | Max 100 |
| offset | integer | Нет | 0 | Для пагинации |
| year_from | integer | Нет | 2020 | YYYY формат |
| year_to | integer | Нет | 2025 | YYYY формат |
| language | array | Нет | ["ru", "en"] | ISO 639-1 |
| concepts | array | Нет | ["education"] | OpenAlex concepts |

### 3.2. Функция: Генерация списка литературы

#### 3.2.1. API Endpoint
```
POST /api/v1/bibliography

Request Body:
{
  "article_ids": [
    "openalex_w123456789",
    "cyberleninka_abc123def",
    "openalex_w987654321"
  ],
  "style": "GOST_R_7_0_100_2018",  // или "VAK", "APA", "HARVARD"
  "sort_by": "year_desc",          // year_asc, year_desc, author, title
  "language": "ru",
  "numbered": true,
  "include_abstracts": false,
  "include_keywords": false
}

Response (200 OK):
{
  "status": "success",
  "formatted_list": [
    "1. Иванов И.И., Петрова П.П. Искусственные нейронные сети в дистанционном образовании // Вестник БГУ. Сер. 4. – 2024. – № 3 (15). – С. 45–56. – DOI: 10.1234/abc-def-123.",
    "2. Сидоров В.В. Основы машинного обучения: учебное пособие. М.: Издательство МГУ, 2023. 350 с."
  ],
  "bibtex": "@article{ivanov2024,\n  author = {Иванов, И.И. and Петрова, П.П.},\n  title = {Искусственные нейронные сети в дистанционном образовании},\n  journal = {Вестник БГУ},\n  year = {2024},\n  volume = {3},\n  number = {15},\n  pages = {45--56},\n  doi = {10.1234/abc-def-123}\n}",
  "ris": "TY  - JOUR\nAU  - Иванов, И.И.\nAU  - Петрова, П.П.\nTI  - Искусственные нейронные сети в дистанционном образовании\nJO  - Вестник БГУ\nVL  - 3\nIS  - 15\nSP  - 45\nEP  - 56\nPY  - 2024\nDO  - 10.1234/abc-def-123\nER  -",
  "word_document_url": "https://api.litfinder.by/export/docx/abc123def",
  "validation": {
    "status": "success",
    "errors": [],
    "warnings": [
      "Источник 2: отсутствует DOI (рекомендуется добавить)"
    ]
  },
  "metadata": {
    "total_sources": 2,
    "source_types": {
      "article": 1,
      "book": 1
    }
  }
}
```

#### 3.2.2. Лимиты генерации (пер-юзер)

| Тариф | ГОСТ генераций/день | Max статей в запросе | Max токенов/запрос |
|--------|-------------------------|------------------------|---------------------|
| Free | **10** | 20 | 4,000 |
| Pro | **100** | 100 | 8,000 |
| Enterprise | **Unlimited** | 500 | 16,000 |

> При исчерпании лимита — HTTP 429 с `Retry-After` header и сообщение: "Дневной лимит генерации исчерпан. Обновите тариф или попробуйте завтра".

### 3.3. Telegram Bot Interface

#### 3.3.1. Команды

```
/start
├─ Приветствие
├─ Краткое объяснение функций
├─ Кнопка "Начать поиск"

/theme "нейросети в образовании"
├─ Поиск в режиме реального времени
├─ Показ первых 5 результатов inline
├─ Кнопки "Еще результаты", "Добавить в список"

/search [query]
├─ Алиас команды /theme

/list
├─ Показать текущий список литературы (до 20 шт)
├─ Кнопка "Генерировать ГОСТ", "Экспортировать"

/export
├─ Выбор формата: ГОСТ | BibTeX | RIS | Word
├─ Отправка файла / текста

/pro
├─ Показать тарифы
├─ Ссылка на веб-интерфейс платежей

/clear
├─ Очистить текущий список
├─ Подтверждение

/help
├─ Справка по командам
```

#### 3.3.2. Inline Buttons (Callback Handler)
```
search_result_[article_id]_add     → Добавить статью в список
search_result_[article_id]_view    → Показать полные данные
search_next                         → Загрузить больше результатов
list_generate_gost                 → Генерировать список по ГОСТ
list_export_bibtex                 → Экспортировать BibTeX
list_clear_confirm                 → Подтвердить очистку
```

### 3.4. Web Interface (MVP)

#### 3.4.1. Страницы

**1. Лэндинг (/)** 
```
- Заголовок: "LitFinder - найдите источники за 2 минуты"
- Краткое описание (3 пункта)
- Скриншоты функций
- CTA: "Попробовать бесплатно" (регистрация/вход)
- Тарифы (таблица)
- FAQ (5-7 вопросов)
```

**2. Поиск (/app/search)**
```
- Поле ввода (input[type=text], placeholder="Введите тему или ключевые слова")
- Фильтры (год, язык, тип источника)
- Кнопка "Поиск" (или Enter)
- Результаты (карточки статей):
  * Название (заголовок)
  * Авторы (список с инициалами)
  * Год, журнал, DOI
  * Аннотация (truncate 100 chars + "Читать далее")
  * Кнопка "Добавить" (чекбокс)
  * Иконка "Открыть PDF" (если доступен)
  * Relevance score (визуальная полоса)
- Пагинация (20 результатов/страница)
```

**3. Мой список (/app/library)**
```
- Заголовок "Мой список литературы"
- Количество источников
- Список с возможностью:
  * Перетаскивания (drag-drop) для переупорядочивания
  * Удаления (Х кнопка)
  * Предпросмотра (expand)
- Выбор стиля оформления (GOST, VAK, APA)
- Кнопки:
  * "Генерировать ГОСТ" → preview
  * "Экспортировать" → выбор формата → скачать
  * "Поделиться" → генерация уникальной ссылки (Pro)
  * "Очистить все" → подтверждение
```

**4. Профиль (/app/profile)**
```
- Email, имя
- История поисков (последние 10)
- Тариф (Free/Pro) с информацией об истечении
- Кнопка "Обновить подписку" (если Free)
- Настройки:
  * Язык интерфейса (RU/EN)
  * Уведомления (email о новых статьях)
  * Экспорт данных (GDPR)
```

---

## 4. ИНТЕГРАЦИИ (ТЕХНИЧЕСКАЯ СПЕЦИФИКАЦИЯ)

### 4.1. OpenAlex API

**Документация:** https://docs.openalex.org  
**Лимиты:** 100 запросов/минуту (freemium), unlimited с email регистрацией  
**Формат:** REST API + JSON  

#### 4.1.1. Используемые endpoints

```
1. GET /works
   Параметры:
   - search: "{query}"
   - filter: "publication_year:>2020"
   - filter: "language:en|ru"
   - per_page: 50
   - page: 1
   
   Используемые поля в response:
   {
     "id": "W123456789",
     "doi": "10.1234/abc",
     "title": "Article title",
     "publication_year": 2024,
     "abstract": "...",
     "authorships": [
       {
         "author": {
           "id": "A123",
           "display_name": "John Smith"
         },
         "institutions": [...]
       }
     ],
     "primary_venue": {
       "id": "V123",
       "display_name": "Journal Name",
       "issn": "1234-5678"
     },
     "open_access": {
       "is_oa": true,
       "oa_status": "gold",
       "oa_url": "https://..."
     },
     "cited_by_count": 12,
     "concepts": [
       {
         "id": "C123",
         "display_name": "Machine Learning",
         "level": 1
       }
     ]
   }

2. GET /works/{id}
   Получение полной информации по одной статье

3. GET /authors
   Поиск авторов (опционально для автодополнения)
```

#### 4.1.2. Маппинг полей в БД

```
OpenAlex            →    LitFinder DB
─────────────────────────────────────
W123456789          →    external_id
10.1234/abc         →    doi
Article title       →    title
[authors...]        →    authors (JSONB)
2024                →    year
Journal Name        →    journal_name
1-50                →    pages
https://...         →    pdf_url
[concepts...]       →    concepts (JSONB)
12                  →    cited_by_count
```

### 4.2. CyberLeninka OAI-PMH

**Протокол:** OAI-PMH v2.0  
**Base URL:** https://cyberleninka.ru/oai  
**Документация:** https://cyberleninka.ru/about#oai  

#### 4.2.1. OAI-PMH запросы

```
1. ListRecords (основной для harvesting)
   URL: https://cyberleninka.ru/oai?verb=ListRecords&metadataPrefix=oai_dc&from=2024-01-01&until=2024-12-31
   
   Response: XML с записями в формате Dublin Core
   <record>
     <header>
       <identifier>oai:cyberleninka.ru:article/123456</identifier>
       <datestamp>2024-12-15T10:30:00Z</datestamp>
     </header>
     <metadata>
       <oai_dc:dc xmlns:oai_dc="http://www.openarchives.org/OAI/2.0/oai_dc/">
         <dc:title>Название статьи</dc:title>
         <dc:creator>Иванов И.И.</dc:creator>
         <dc:creator>Петрова П.П.</dc:creator>
         <dc:date>2024-12-15</dc:date>
         <dc:identifier>urn:cyberleninka:article:123456</dc:identifier>
         <dc:identifier>https://cyberleninka.ru/article/n/123456</dc:identifier>
         <dc:source>Вестник БГУ</dc:source>
         <dc:language>ru</dc:language>
         <dc:subject>Образование; ИИ</dc:subject>
         <dc:description>Краткое описание...</dc:description>
       </oai_dc:dc>
     </metadata>
   </record>

2. GetRecord (для получения конкретной записи)
   URL: https://cyberleninka.ru/oai?verb=GetRecord&identifier=oai:cyberleninka.ru:article/123456&metadataPrefix=oai_dc

3. ListIdentifiers (быстрый способ получить только ID)
```

#### 4.2.2. Маппинг Dublin Core → БД

```
OAI-DC            →    LitFinder DB
─────────────────────────────────────
identifier        →    external_id
title             →    title
creator(s)        →    authors (array)
date              →    year
source            →    journal_name
dc:description    →    abstract
dc:identifier URL →    url_cyberleninka
```

#### 4.2.3. Harvesting Strategy

```
Периодичность: Ежедневно в 02:00 UTC
Инкрементальный harvest:
  from={today-1}.T00:00:00Z
  until={today}.T23:59:59Z

Хранение:
  - resumptionToken для продолжения
  - Кэш на 24 часа
  - Дедубликация по DOI/URL

Rate limit:
  - 10 запросов/сек (OAI-PMH рекомендация)
  - Retry при 503 с exponential backoff
```

### 4.3. Claude API (LLM)

**Модель:** Claude Haiku 4.5 (MVP) / Sonnet 3.5 (Pro)  
**Документация:** https://platform.claude.com/docs  
**Цена:** $0.25/млн input + $1.25/млн output (Haiku)  

#### 4.3.1. Использование в MVP

**Задача 1: Переформулировка запроса**
```
System Prompt:
"Ты — помощник для формулирования академических поисков. 
Твоя задача: переформулировать пользовательский запрос 
в набор ключевых слов для поиска научных статей."

User Input:
"Как ИИ меняет образование?"

Expected Output:
{
  "keywords": ["artificial intelligence", "education", "machine learning"],
  "concepts": ["AI", "digital transformation", "teaching"],
  "languages": ["ru", "en"],
  "year_range": [2020, 2025]
}
```

**Задача 2: Нормализация ГОСТ**
```
System Prompt:
"Ты — эксперт по ГОСТ Р 7.0.100-2018. 
Твоя задача: форматировать библиографические данные по ГОСТу."

User Input:
{
  "authors": ["Ivan Ivanov", "Petr Petrov"],
  "title": "AI in Education",
  "journal": "Bulletin of BSU",
  "year": 2024,
  "pages": "45-56"
}

Expected Output:
"Иванов И.И., Петров П.П. Искусственный интеллект в образовании 
// Вестник БГУ. Сер. 4. – 2024. – № 3. – С. 45–56."
```

#### 4.3.1. Rate Limiting

```
Free tier (MVP): до 100k tokens/день (общий бюджет)
Pro tier: unlimited

Пер-юзер лимиты (Claude API):
  ┌────────────────────────────────────────────────────────┐
  │  Параметр          │  Free    │ Pro     │ Enterprise │
  ├────────────────────┼─────────┼─────────┼────────────┤
  │ ГОСТ ген./день     │  10     │ 100     │ Unlimited  │
  │ Max статей/запрос  │  20     │ 100     │ 500        │
  │ Max tokens/запрос  │  4,000  │ 8,000   │ 16,000     │
  │ Поисков /день      │  10     │ 1,000   │ Unlimited  │
  └────────────────────┴─────────┴─────────┴────────────┘

Защита от злоупотреблений:
  - Трекинг: Redis INCR user:{id}:claude_calls (TTL 86400s)
  - При >80% лимита → предупреждение пользователю
  - При 100% → HTTP 429 + предложение апгрейда до Pro
  - Аномальная активность (>50 запр/час) → алерт в Telegram

Cache:
  - Redis TTL 24ч для одинаковых запросов
  - Сокращение стоимости ~90%
  - Кэшированные запросы не считаются в пер-юзер лимит
```

### 4.4. Embedding Service (OpenAI)

**Модель:** `text-embedding-3-small` (OpenAI)  
**Размерность:** 1536 (соответствует `VECTOR(1536)` в таблице `articles`)  
**Endpoint:** `https://api.openai.com/v1/embeddings`  
**Auth:** Bearer token (`OPENAI_API_KEY`)  

#### 4.4.1. Решение по таймингу

```
Стратегия: Гибридная (precompute + on-query)

1. PRECOMPUTE на ingestion (основной путь):
   → При harvesting из OpenAlex/CyberLeninka:
     title + abstract → embedding → сохранение в articles.embedding
   → Batch API: до 2048 текстов за один вызов
   → Celery worker обрабатывает очередь в фоне

2. ON-QUERY для поискового запроса:
   → Пользовательский запрос → embedding → pgvector cosine similarity
   → Один вызов API на каждый поисковый запрос
   → Кэш в Redis (TTL 24ч) для повторных запросов
```

#### 4.4.2. API интеграция

```python
# Пример вызова
import openai

response = openai.embeddings.create(
    model="text-embedding-3-small",
    input=["текст статьи для векторизации"],
    dimensions=1536  # явное указание размерности
)
vector = response.data[0].embedding  # list[float] длиной 1536
```

#### 4.4.3. Лимиты, стоимость и обработка ошибок

| Параметр | Значение |
|----------|----------|
| Rate limit | 3,000 RPM / 1,000,000 TPM |
| Max input | 8,191 токенов на текст |
| Стоимость | $0.020 / 1M токенов |
| Latency | ~50-100ms на запрос |
| **Оценка MVP** | **~100k статей × ~200 токенов = 20M токенов = $0.40** |

```
Обработка ошибок:
  - HTTP 429 (Rate limit) → exponential backoff (1s, 2s, 4s, max 60s)
  - HTTP 500/503 → retry 3 раза, затем fallback на детерминистический embedding
  - Timeout (>5s) → cancel + retry
  - Невалидный input (>8191 tokens) → truncate до 8000 токенов

Fallback (без API):
  - Детерминистический embedding на основе TF-IDF + хеширования
  - Качество ниже, но система остаётся работоспособной
```

---

## 5. ТЕХНИЧЕСКИЙ СТЕК

### 5.1. Backend

```
Framework:      FastAPI 0.104+ (Python 3.11+)
Database:       PostgreSQL 16 (Yandex Managed)
Vector Search:  pgvector 0.5+
ORM:            SQLAlchemy 2.0
Async:          asyncio + aiohttp
Caching:        Redis 7.0+
Task Queue:     Celery + RabbitMQ (для harvesting)
LLM Client:     anthropic-sdk (Python)
Embeddings:     openai (Python SDK) → text-embedding-3-small
```

### 5.2. Frontend

```
Framework:      React 18 + Next.js 14
Build tool:     Webpack/Turbopack
Styling:        Tailwind CSS 3.4
UI Components:  Shadcn/ui или Material-UI
HTTP Client:    axios + react-query
State:          Zustand или Redux Toolkit
```

### 5.3. Telegram Bot

```
Library:        aiogram 3.x (async)
Database:       PostgreSQL (shared с API)
Webhook:        FastAPI endpoint /tg/webhook
```

### 5.4. Infrastructure & DevOps

```
Containerization:   Docker + docker-compose
Orchestration:      Kubernetes (future) / простые VMs (MVP)
Cloud Provider:     Yandex Cloud (by location)
Monitoring:         Prometheus + Grafana
Logging:            ELK Stack (Elasticsearch/Logstash/Kibana)
Error Tracking:     Sentry
CI/CD:              GitHub Actions
Package Registry:   GitHub Container Registry (ghcr.io)
```

### 5.5.架构图 (ASCII)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Клиенты                                 │
├──────────┬──────────────────────┬───────────────┬──────────────┤
│  Web App │  Telegram Bot        │  API Mobile   │  Документы   │
│(React)  │  (@litfinder_bot)    │  (REST)       │  (Export)    │
└────┬─────┴───────────┬──────────┴───────┬───────┴──────┬───────┘
     │                 │                  │              │
     └─────────────────┼──────────────────┼──────────────┘
                       ▼
          ┌────────────────────────┐
          │  API Gateway (FastAPI) │ ← Rate limiting, auth
          │  /api/v1/*             │
          └────────────┬───────────┘
                       │
        ┌──────────────┼──────────────────┐
        ▼              ▼                  ▼
    ┌────────┐    ┌──────────┐    ┌────────────────┐
    │ Search │    │ Format   │    │ User Service   │
    │ Service│    │ Service  │    │ (Auth, Billing)│
    └────┬───┘    └────┬─────┘    └────────┬───────┘
         │             │                   │
         └─────────────┼───────────────────┘
                       │
    ┌──────────────────┼──────────────────┐
    │                  │                  │
    ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────┐
│  PostgreSQL 16 (Yandex Managed)                    │
│  ├─ articles (с pgvector embeddings)               │
│  ├─ users (профили, подписки)                      │
│  ├─ search_history                                 │
│  └─ bibliography_lists                             │
└──────────────────────┬────────────────────────────┘
                       │
     ┌──────────────────┼──────────────────┐
     ▼                  ▼                  ▼
 ┌────────┐    ┌──────────────┐    ┌────────────┐
 │ Redis  │    │ OpenAlex API │    │CyberLeninka│
 │(Cache) │    │              │    │ OAI-PMH    │
 └────────┘    └──────────────┘    └────────────┘
                    │                     │
          ┌─────────┼─────────────────────┘
          ▼         ▼
     ┌──────────────────────┐
     │  Embedding Service   │ ← text-embedding-3-small (1536-d)
     │  (OpenAI API)        │   Precompute on ingestion +
     │  └→ pgvector cosine  │   On-demand for queries
     └──────────────────────┘
                    │
               ┌────▼────┐
               │Claude   │ ← ГОСТ formatting,
               │API      │   query expansion
               └─────────┘
```

---

## 6. НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 6.1. Производительность

| Метрика | Требование | Способ измерения |
|---------|-----------|-----------------|
| Время ответа поиска | ≤2 сек (95-перцентиль) | Prometheus latency |
| Время экспорта в ГОСТ | ≤1 сек | API response time |
| Throughput | ≥20 req/сек | Load test (Artillery) |
| Поиск в памяти (cache hit) | ≤100ms | Redis metrics |

### 6.2. Надежность & Доступность

```
SLA: 99.5% uptime (≤3.6 часа downtime/месяц)
RTO (Recovery Time Objective): 15 минут
RPO (Recovery Point Objective): 1 час

Мониторинг:
  - Health checks каждые 30 сек
  - Database replication (primary-replica)
  - Автоматический failover
  - Резервные копии каждый час
```

#### 6.2.1. Политика резервного копирования (Backup Retention Policy)

**Расписание и ротация:**

| Тип бэкапа | Частота | Хранение | Тип хранилища |
|---|---|---|---|
| Hourly snapshots (PostgreSQL WAL) | Каждый час | **24 часа** (последние 24 копии) | Hot storage (SSD, тот же регион) |
| Daily full backup | Ежедневно в 02:00 UTC | **7 дней** | Warm storage (HDD, тот же регион) |
| Weekly full backup | Воскресенье 03:00 UTC | **4 недели** | Cold storage (Yandex Object Storage, другой регион) |
| Monthly archive | 1-е число месяца | **6 месяцев** | Cold storage (Yandex Object Storage, geo-redundant) |

**Хранилище:**

```
Primary region:  ru-central1-a (Yandex Cloud)
DR region:       ru-central1-b (weekly + monthly бэкапы)

Оценка объёма хранения (MVP):
  - PostgreSQL dump: ~500 MB (10k пользователей)
  - Hourly (24 × 500MB):    ~12 GB   → SSD: ~120 ₽/мес
  - Daily  (7 × 500MB):     ~3.5 GB  → HDD: ~7 ₽/мес
  - Weekly (4 × 500MB):     ~2 GB    → Object Storage: ~2 ₽/мес
  - Monthly (6 × 500MB):    ~3 GB    → Object Storage: ~3 ₽/мес
  ─────────────────────────────────────
  Итого backup storage:     ~20.5 GB → ~132 ₽/мес (~$1.50)
```

### 6.3. Масштабируемость

```
Текущая инфраструктура (MVP):
  - 1 API инстанс (Yandex Cloud, 4 vCPU, 8 GB RAM)
  - Managed PostgreSQL (8 vCPU, 32 GB RAM, 200 GB SSD)
  - Redis (2 GB)
  
При 10k DAU (месяц 6):
  - Горизонтальное масштабирование API (load balancer)
  - Read replicas для PostgreSQL
  - Redis Cluster
```

### 6.4. Безопасность

```
✅ Требования:
  - HTTPS everywhere (TLS 1.3)
  - API Key authentication (для бота/мобильных клиентов)
  - JWT для веб-сессий (exp: 24h)
  - Хранение паролей (bcrypt + salt)
  - Rate limiting: 100 req/мин без auth, 1000 с auth
    Response headers:
      X-RateLimit-Limit: 100
      X-RateLimit-Remaining: 97
      X-RateLimit-Reset: 1710000000 (Unix timestamp)
    При превышении: HTTP 429 + Retry-After header
  - SQL injection prevention (ORM + parameterized queries)
  - CORS только для доверенных доменов
  - Логирование всех изменений (audit log)

🔐 Credential Management:
  - API ключи в Vault (Hashicorp или аналог)
  - Environment variables для продакшена
  - Ротация ключей каждый квартал
```

### 6.5. Соответствие стандартам

```
✅ GDPR compliance:
  - Право на удаление (delete endpoint)
  - Экспорт данных пользователя (JSON)
  - Privacy Policy + Terms of Service
  - Логирование согласия на обработку ПДн

✅ Беларусь:
  - Локализация в соответствии с местным законодательством
  - Возможность хостинга данных внутри BY
```

#### 6.5.1. Политика хранения данных (Data Retention Policy)

> В соответствии с GDPR Article 5(1)(e) — принцип ограничения хранения (storage limitation).

| Категория данных | Срок хранения | Действие по истечении |
|---|---|---|
| `search_history` | **90 дней** | Автоматическое удаление (cron job) |
| `users` (soft-deleted, `deleted_at IS NOT NULL`) | **30 дней** | Hard delete (каскадное удаление связанных данных) |
| `bibliography_lists` после удаления пользователя | **30 дней** (вместе с пользователем) | Каскадное удаление (`ON DELETE CASCADE`) |
| Audit logs (действия администраторов) | **1 год** | Архивация в cold storage, затем удаление |
| Session tokens / refresh tokens | **7 дней** / **30 дней** | Автоматическая инвалидация |
| Кэш поисковых результатов (Redis) | **24 часа** | TTL auto-expiry |
| Экспортированные файлы (DOCX, BibTeX) | **7 дней** | Удаление из temp storage |

```
📋 Реализация:
  - Cron-задача: ежедневно в 03:00 UTC
  - DELETE FROM search_history WHERE created_at < now() - INTERVAL '90 days'
  - DELETE FROM users WHERE deleted_at < now() - INTERVAL '30 days'
  - Уведомление пользователю за 7 дней до hard delete (email/Telegram)
  - Endpoint: GET /api/v1/user/export (GDPR data portability)
  - Endpoint: DELETE /api/v1/user (запускает soft delete + 30-дневный countdown)
```

---

## 7. СТРУКТУРА БАЗЫ ДАННЫХ

### 7.1. ER Диаграмма

```
users                           articles
├─ id (UUID, PK)               ├─ id (UUID, PK)
├─ telegram_id (BigInt, UNQ)   ├─ source (VARCHAR)
├─ email (VARCHAR, UNQ)        ├─ external_id (VARCHAR)
├─ name (VARCHAR)              ├─ title (TEXT)
├─ password_hash (VARCHAR)     ├─ authors (JSONB)
├─ subscription_tier           ├─ year (INT)
│  (free|pro|enterprise)       ├─ journal_name (VARCHAR)
├─ subscription_expires (TS)   ├─ volume (INT)
├─ created_at (TS)             ├─ issue (INT)
├─ updated_at (TS)             ├─ pages (VARCHAR)
├─ deleted_at (TS, soft)       ├─ doi (VARCHAR, UNQ)
└─ metadata (JSONB)            ├─ pdf_url (VARCHAR)
                               ├─ abstract (TEXT)
searches (history)             ├─ concepts (JSONB)
├─ id (UUID, PK)               ├─ cited_by_count (INT)
├─ user_id (FK → users)        ├─ open_access (BOOL)
├─ query (TEXT)                ├─ embedding (VECTOR(1536))    ← OpenAI text-embedding-3-small
├─ results_count (INT)         ├─ created_at (TS)
├─ execution_ms (INT)          └─ harvested_from (VARCHAR)
├─ created_at (TS)
└─ metadata (JSONB)

bibliography_lists            bibliography_items
├─ id (UUID, PK)              ├─ id (UUID, PK)
├─ user_id (FK → users)       ├─ list_id (FK → bibliography_lists)
├─ name (VARCHAR)             ├─ article_id (FK → articles)
├─ description (TEXT)         ├─ position (INT)
├─ style (GOST|VAK|APA)       ├─ notes (TEXT)
├─ shared_with (JSONB)        └─ created_at (TS)
├─ public_link (VARCHAR, UNQ)
├─ created_at (TS)
├─ updated_at (TS)
└─ deleted_at (TS)
```

### 7.2. SQL Schema

```sql
-- Таблица articles
CREATE TABLE articles (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  source VARCHAR(50) NOT NULL,  -- 'openalex', 'cyberleninka'
  external_id VARCHAR(100) NOT NULL,
  source_external_id VARCHAR(100) UNIQUE,
  title TEXT NOT NULL,
  authors JSONB NOT NULL DEFAULT '[]',  -- [{"name": "...", "initials": "..."}, ...]
  year INTEGER,
  journal_name VARCHAR(255),
  volume INTEGER,
  issue INTEGER,
  pages VARCHAR(50),
  doi VARCHAR(100) UNIQUE,
  pdf_url TEXT,
  abstract TEXT,
  abstract_snippet TEXT,
  concepts JSONB DEFAULT '[]',  -- [{"id": "...", "name": "..."}, ...]
  cited_by_count INTEGER DEFAULT 0,
  open_access BOOLEAN DEFAULT false,
  language VARCHAR(10) DEFAULT 'ru',
  embedding VECTOR(1536),  -- pgvector embeddings
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now(),
  harvested_from VARCHAR(50) DEFAULT 'manual',
  
  CONSTRAINT check_source CHECK (source IN ('openalex', 'cyberleninka', 'manual')),
  INDEX idx_title ON articles USING gin(to_tsvector('russian'::regconfig, title)),
  INDEX idx_abstract ON articles USING gin(to_tsvector('russian'::regconfig, abstract)),
  INDEX idx_year ON articles(year DESC),
  INDEX idx_embedding ON articles USING ivfflat (embedding vector_cosine_ops)
);

-- Таблица users
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  telegram_id BIGINT UNIQUE,
  email VARCHAR(100) UNIQUE,
  name VARCHAR(255),
  password_hash VARCHAR(255),
  subscription_tier VARCHAR(20) DEFAULT 'free',  -- free, pro, enterprise
  subscription_expires TIMESTAMP,
  search_limit_daily INTEGER DEFAULT 10,  -- free: 10, pro: 1000
  searches_used_today INTEGER DEFAULT 0,
  searches_reset_at TIMESTAMP DEFAULT now() + INTERVAL '1 day',
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now(),
  deleted_at TIMESTAMP,  -- soft delete
  metadata JSONB DEFAULT '{}',  -- {language: "ru", notifications: true, ...}
  
  CONSTRAINT check_tier CHECK (subscription_tier IN ('free', 'pro', 'enterprise'))
);

-- Таблица bibliography_lists
CREATE TABLE bibliography_lists (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  style VARCHAR(50) DEFAULT 'GOST_R_7_0_100_2018',
  public_link VARCHAR(100) UNIQUE,
  is_public BOOLEAN DEFAULT false,
  shared_with JSONB DEFAULT '[]',  -- [{"user_id": "...", "access": "view"}]
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now(),
  deleted_at TIMESTAMP  -- soft delete
);

-- Таблица bibliography_items
CREATE TABLE bibliography_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  list_id UUID NOT NULL REFERENCES bibliography_lists(id) ON DELETE CASCADE,
  article_id UUID NOT NULL REFERENCES articles(id) ON DELETE CASCADE,
  position INTEGER NOT NULL,
  notes TEXT,
  created_at TIMESTAMP DEFAULT now(),
  
  UNIQUE(list_id, article_id)
);

-- Таблица search_history
CREATE TABLE search_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  query TEXT NOT NULL,
  results_count INTEGER,
  filters JSONB,
  execution_ms INTEGER,
  created_at TIMESTAMP DEFAULT now(),
  
  INDEX idx_user_created ON search_history(user_id, created_at DESC)
);

-- Таблица для логирования платежей (future)
CREATE TABLE payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  amount DECIMAL(10,2),
  currency VARCHAR(3) DEFAULT 'USD',
  status VARCHAR(20),  -- pending, completed, failed
  payment_method VARCHAR(50),  -- stripe, bepaid, fondy
  external_transaction_id VARCHAR(255),
  created_at TIMESTAMP DEFAULT now()
);
```

---

## 8. API СПЕЦИФИКАЦИЯ

### 8.1. OpenAPI/Swagger

```yaml
openapi: 3.0.0
info:
  title: LitFinder API
  version: 1.0.0
  description: ИИ-платформа подбора академической литературы

servers:
  - url: https://api.litfinder.by/api/v1
    description: Production
  - url: http://localhost:8000/api/v1
    description: Development

paths:
  /search:
    post:
      summary: Поиск статей
      tags: [Search]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
      responses:
        '200':
          description: Успешный поиск
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          description: Неверные параметры
        '429':
          description: Превышен лимит запросов
      security:
        - ApiKeyAuth: []
        - BearerAuth: []

components:
  schemas:
    SearchRequest:
      type: object
      required: [query]
      properties:
        query:
          type: string
          minLength: 3
          example: "нейросети в образовании"
        limit:
          type: integer
          default: 20
          maximum: 100
        offset:
          type: integer
          default: 0
        filters:
          type: object
          properties:
            year_from:
              type: integer
              example: 2020
            year_to:
              type: integer
              example: 2025
            language:
              type: array
              items:
                type: string
                enum: [ru, en]
            concepts:
              type: array
              items:
                type: string

    SearchResponse:
      type: object
      properties:
        total:
          type: integer
        results:
          type: array
          items:
            $ref: '#/components/schemas/Article'
        execution_time_ms:
          type: integer

    Article:
      type: object
      properties:
        id:
          type: string
          format: uuid
        source:
          type: string
          enum: [openalex, cyberleninka]
        title:
          type: string
        authors:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              initials:
                type: string
        year:
          type: integer
        journal:
          type: string
        doi:
          type: string
        pdf_url:
          type: string
          format: uri
        relevance_score:
          type: number
          format: float
          minimum: 0
          maximum: 1

  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
```

### 8.2. Основные Endpoints

```
GET  /health                    Статус сервиса
POST /search                    Поиск статей
POST /bibliography              Генерация списка литературы (лимит: 10/день Free, 100 Pro)
POST /bibliography/validate     Валидация списка
GET  /bibliography/{id}         Получить список литературы
DELETE /bibliography/{id}       Удалить список
GET  /user/usage                Статистика (вкл. остаток лимитов Claude)

POST /auth/register             Регистрация
POST /auth/login                Вход
POST /auth/logout               Выход
POST /auth/refresh              Обновить токен

GET  /user/profile              Профиль пользователя
PUT  /user/profile              Обновить профиль
GET  /user/searches             История поисков
GET  /user/usage                Статистика использования

POST /export/{format}           Экспорт в формат (docx, pdf, bibtex, ris)

GET  /tg/webhook                Telegram webhook endpoint
```

### 8.3. Стандартный формат ошибок

```json
{
  "error": {
    "code": "SEARCH_QUERY_TOO_SHORT",
    "message": "Поисковый запрос должен содержать минимум 3 символа",
    "status": 400,
    "timestamp": "2026-01-22T12:00:00Z",
    "request_id": "req_abc123"
  }
}
```

**Коды ошибок:**

| HTTP | Code | Описание |
|------|------|----------|
| 400 | `INVALID_REQUEST` | Невалидные параметры |
| 400 | `SEARCH_QUERY_TOO_SHORT` | Запрос < 3 символов |
| 401 | `UNAUTHORIZED` | Отсутствует или невалидный токен |
| 403 | `SUBSCRIPTION_LIMIT` | Превышен лимит тарифа |
| 404 | `NOT_FOUND` | Ресурс не найден |
| 429 | `RATE_LIMITED` | Превышен лимит запросов |
| 500 | `INTERNAL_ERROR` | Внутренняя ошибка сервера |
| 502 | `UPSTREAM_ERROR` | Ошибка внешнего API (OpenAlex/CyberLeninka) |
| 503 | `SERVICE_UNAVAILABLE` | Сервис временно недоступен |

---

## 9. ТЕСТИРОВАНИЕ И ПРИЕМКА

### 9.1. Уровни тестирования

#### 9.1.1. Unit-тесты (80% покрытие)
```
✅ search_service.py
   - Тестирование запросов к OpenAlex (mock)
   - Ранжирование результатов
   - Фильтрация по году/языку

✅ gost_formatter.py
   - Форматирование статей (100+ шаблонов)
   - Обработка неполных данных
   - Генерация номеров

✅ metadata_extractor.py
   - Парсинг OAI-PMH ответов
   - Валидация полей
   - Нормализация авторов

✅ telegram_handlers.py
   - Обработка команд (/search, /list, /export)
   - Inline button callbacks
   - Rate limiting
```

#### 9.1.2. Интеграционные тесты
```
✅ End-to-End поиск:
   1. POST /search с запросом
   2. OpenAlex API вызывается
   3. Результаты сохраняются в DB
   4. Возвращается корректный ответ

✅ End-to-End генерация ГОСТ:
   1. POST /bibliography с ID статей
   2. Метаданные извлекаются из DB
   3. Claude API форматирует по ГОСТ
   4. Возвращается правильный формат
```

#### 9.1.3. Load Testing
```
Инструмент: Apache JMeter / Artillery.io

Сценарии:
1. Базовая нагрузка: 10 RPS × 300 сек
2. Пиковая нагрузка: 50 RPS × 60 сек
3. Стресс-тест: 100 RPS до failure

Метрики:
  - Latency P95: ≤2000ms
  - Error rate: <1%
  - Throughput: ≥50 req/sec
```

### 9.2. Критерии приемки MVP

```
✅ Функциональность (Must Have)
  [ ] Поиск в OpenAlex работает (>95% recall на 100 запросов)
  [ ] Поиск в CyberLeninka работает (>90%)
  [ ] Генерация ГОСТ без ошибок (экспертная проверка)
  [ ] Telegram-бот отвечает <30 сек
  [ ] Web UI адаптивен (desktop + mobile)
  [ ] Экспорт во все форматы работает

✅ Производительность
  [ ] Среднее время ответа поиска ≤2 сек
  [ ] Генерация списка ≤1 сек
  [ ] Uptime ≥99%

✅ Безопасность
  [ ] HTTPS везде
  [ ] SQL injection tests пройдены
  [ ] Rate limiting работает
  [ ] GDPR compliance достигнут

✅ UX/UI
  [ ] NPS ≥70 на 30 beta-пользователях
  [ ] Time-to-first-result ≤15 сек
  [ ] Отсутствие критических багов (Sentry)

✅ Документация
  [ ] API Swagger docs полные
  [ ] Deployment guide написан
  [ ] README в GitHub
```

### 9.3. Testing Checklist

```
[ ] Зарегистрировать аккаунт (Free)
[ ] Выполнить 10 поисков (дневной лимит)
[ ] Попробовать 11-й поиск (error)
[ ] Добавить 5 статей в список
[ ] Генерировать ГОСТ (скопировать текст)
[ ] Генерировать BibTeX
[ ] Скачать Word документ
[ ] Поделиться списком (Pro feature)
[ ] Открыть через Telegram-бота (/theme)
[ ] Очистить список (/clear)
[ ] Обновить подписку на Pro
[ ] Отправить feedback
```

---

## 10. ДЕПЛОЙ И ЭКСПЛУАТАЦИЯ

### 10.1. Docker Setup (для локальной разработки)

```dockerfile
# Dockerfile backend
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```yaml
# docker-compose.yml
version: '3.8'

services:
  api:
    build: ./backend
    container_name: litfinder_api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/litfinder
      - OPENALEX_EMAIL=${OPENALEX_EMAIL}
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:
    image: pgvector/pgvector:pg16
    container_name: litfinder_db
    environment:
      POSTGRES_USER: litfinder_user
      POSTGRES_PASSWORD: litfinder_password
      POSTGRES_DB: litfinder
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db_init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    container_name: litfinder_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  web:
    build: ./frontend
    container_name: litfinder_web
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
    depends_on:
      - api

volumes:
  postgres_data:
  redis_data:
```

### 10.2. Yandex Cloud Deployment

```yaml
# k8s manifests (для будущего масштабирования)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: litfinder-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: litfinder-api
  template:
    metadata:
      labels:
        app: litfinder-api
    spec:
      containers:
      - name: api
        image: ghcr.io/yourorg/litfinder-api:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: litfinder-secrets
              key: database_url
        - name: CLAUDE_API_KEY
          valueFrom:
            secretKeyRef:
              name: litfinder-secrets
              key: claude_api_key
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
```

### 10.3. Мониторинг и Логирование

```
📊 Prometheus metrics:
  - http_request_duration_seconds (histogram)
  - http_requests_total (counter)
  - database_query_duration_seconds
  - cache_hits_total / cache_misses_total
  - claude_api_tokens_used

📋 Sentry error tracking:
  - DSN: https://your-sentry-dsn@sentry.io/123456
  - Release: v1.0.0
  - Environment: production/staging

🔍 ELK Stack (опционально):
  - Kibana dashboard для анализа логов
  - Index: litfinder-*-YYYY.MM.DD

📈 Grafana dashboards:
  - API Performance
  - Database Health
  - User Activity
  - Billing & Usage
```

### 10.3.1. Пороги алертов

| Метрика | Warning | Critical | Канал |
|---------|---------|----------|-------|
| API latency P95 | > 3s | > 5s | Telegram + Email |
| Error rate (5xx) | > 1% | > 5% | Telegram + PagerDuty |
| CPU usage | > 70% | > 90% | Email |
| Memory usage | > 75% | > 90% | Telegram |
| Disk usage | > 80% | > 95% | Email + Telegram |
| DB connection pool | > 80% занято | > 95% | Telegram |
| OpenAlex API errors | > 10/час | > 50/час | Telegram |
| Backup failure | любой сбой | 2+ подряд | Telegram + Email |

```
📞 Эскалация:
  1. Warning → Slack/Telegram notification (автоматически)
  2. Critical → SMS on-call дежурному + автоматический rollback
  3. >15 мин без ответа → эскалация на Lead/CTO
```

### 10.4. Checklist Development → Production

```
Pre-launch:
[ ] Все тесты passing (100% критичных кейсов)
[ ] Code review by senior dev
[ ] Security audit completed
[ ] Database backup tested
[ ] Load test passed (50 RPS)
[ ] Monitoring configured
[ ] Alerting configured (email/Slack)
[ ] Runbooks написаны
[ ] Disaster recovery plan готов

Deployment:
[ ] Docker image pushed to registry
[ ] Kubernetes manifests applied
[ ] Health checks passing
[ ] Smoke tests OK
[ ] Beta users notified

Post-launch (24 часа):
[ ] Monitor error rate (<0.5%)
[ ] Check latency P95 (<2s)
[ ] Validate feature flags
[ ] Review user feedback
[ ] Scale if needed
```

---

## 11. РИСКИ И ПЛАН B

### 11.1. Основные риски

| # | Риск | Вероятность | Impact | Mitigation |
|---|------|-------------|--------|------------|
| 1 | OpenAlex API downtime | Средняя | Высокий | Кэш результатов на 48ч; fallback на CyberLeninka |
| 2 | Задержка в интеграции CyberLeninka | Средняя | Средний | Начать с OpenAlex; OAI-PMH стабилен |
| 3 | Claude API затраты >бюджета | Низкая | Средний | Кэширование запросов; Haiku дешевле Sonnet; rate limiting |
| 4 | Низкий adoption (<100 пользователей) | Высокая | Высокий | Ручной онбординг 50 аспирантов БГУ/БНТУ; viral loop |
| 5 | ГОСТ-форматирование ошибки | Средняя | Высокий | Expert validation; Claude перепроверяет; feedback loop |
| 6 | Блокировка в РФ (политические причины) | Низкая | Высокий | VPN-обход; локальный хостинг; альтернативные домены |
| 7 | Конкуренция (Kampus.ai расширяется) | Высокая | Средний | Fokus на аспирантов (они не целевая аудитория Kampus); ГОСТ |

### 11.2. Plan B по функциям

```
Если OpenAlex API недоступен:
  → Использовать только CyberLeninka (50% функциональности)
  → Отключить поиск по English-only статьям
  → Показать message: "Часть источников временно недоступна"

Если Claude API дорогой:
  → Переключиться на Haiku-only (экономия 80%)
  → Использовать Ollama locally (Mistral 7B) для простых операций
  → Кэширование всех запросов

Если низкий adoption:
  → Рассмотреть B2B: продать вузам (БНТУ, БГУ)
  → Интеграция с LMS (Moodle)
  → Программа рефералов

Если ЭЛИБРАРи API требования жесткие:
  → Договор только на коммерческое использование
  → Альтернатива: собственное crawling (рискованно)
```

### 11.3. Graceful Degradation (деградация сервиса)

| Внешний сервис | Индикация отказа | Поведение при сбое | Время восстановления |
|---|---|---|---|
| OpenAlex API | > 3 timeout подряд | ⚠️ Поиск только по CyberLeninka + кэш. Баннер: "Часть источников временно недоступна" | Retry каждые 60 сек, circuit breaker (5 мин) |
| CyberLeninka OAI-PMH | HTTP 5xx / timeout | ⚠️ Поиск только по OpenAlex. Русскоязычные статьи ограничены | Auto-retry с exponential backoff |
| Claude API | 429 / 5xx | ⚠️ ГОСТ форматирование по шаблонам (без AI). Сообщение: "Упрощённое форматирование" | Fallback на локальные шаблоны; retry через 5 мин |
| PostgreSQL | Connection refused | 🔴 Полный read-only mode из Redis кэша. 503 для write-операций | Автоматический failover на replica (< 30 сек) |
| Redis | Connection lost | ⚠️ Работает без кэша (повышенная latency). Rate limiting отключён | Reconnect каждые 5 сек |

---

## 12. БЮДЖЕТ И СРОКИ

### 12.1. Временной план (12 недель)

```
Недели 1-2:    Подготовка, архитектура, setup
Недели 3-6:    Backend разработка (поиск, ГОСТ)
Недели 7-9:    Frontend + Telegram bot
Недели 10-12:  Testing, beta, deployment

Timeline to Market: 12 недель
Break-even: месяц 8 (300 Pro users @ $5/мес = $1.5k MRR)
```

### 12.2. Бюджет MVP (35k USD)

```
Персонал (3-4 чел × 3 месяца):
  - Fullstack developer (3k USD/мес) ............ 9k
  - ML engineer (4k USD/мес) ..................  12k
  - Product Manager/Founder (фаундер) .........  0

Инфраструктура & сервисы:
  - Yandex Cloud (1k USD/мес × 3 мес) .........  3k
  - Claude API credits .........................  1k
  - Domain, SSL, CDN ...........................  500

Другое:
  - QA testing, beta-feedback ..................  2k
  - Legal/регистрация ...........................  1k
  - Marketing материалы .........................  1.5k
  
ИТОГО: ~30-35k USD
```

---

## 13. УСПЕХ-МЕТРИКИ И АНАЛИТИКА

### 13.1. Product Metrics (для MVP)

```
Awareness:
  - Organic traffic (Google Search) по "ГОСТ список литературы"
  - Telegram cannel subscribers: целевая 1000
  - Reddit mentions в r/graduateschool

Acquisition (CAC target: <$3):
  - Sign-ups через веб (target: 1000/месяц к месяцу 3)
  - Telegram bot users (target: 500/месяц)
  - Google Scholar traffic (viral)

Activation (Aha moment):
  - Первый успешный поиск: целевая 40% в день регистрации
  - Первый сгенерированный список: целевая 15%

Retention (D7: 35%, D30: 20%):
  - DAU/MAU ratio: целевая 30%
  - Frequency: >2 поисков/неделю

Monetization (ARPU целевая $5-10):
  - Pro conversion rate: 10-15% (от активных)
  - Churn rate: <10%/месяц (за исключением Free)

Referral (будущее):
  - Viral coefficient: >1.2 (каждый платящий bring 1.2 новых)
```

### 13.2. Technical Metrics (мониторинг)

```
Performance:
  - API latency P95: целевая <2s
  - Error rate: целевая <0.5%
  - Database query time P95: целевая <200ms

Reliability:
  - Uptime: целевая 99.5%
  - MTTR (Mean Time To Recover): целевая <15 мин
  - Backup success rate: 100%

Scalability:
  - Max concurrent users: целевая 100 → 1000 (месяц 6)
  - Database size: целевая 10M articles (месяц 6)
```

---

## 14. КОНТАКТЫ И ОТВЕТСТВЕННЫЕ

| Роль | Имя | Email | Ответственность |
|------|------|-------|-----------------|
| Product Owner | [Вы] | ... | Видение, приоритеты |
| Lead Backend | [Имя] | ... | Архитектура, API |
| Lead Frontend | [Имя] | ... | Web UI, UX |
| DevOps | [Имя] | ... | Инфра, мониторинг |
| QA Lead | [Имя] | ... | Тестирование |

---

## 15. ПРИЛОЖЕНИЯ

### A. Шаблоны ГОСТ (полный справочник)

```
ГОСТ Р 7.0.100–2018 - Статья в журнале
Фамилия И.О., Фамилия И.О. Название статьи // Название журнала. – Год. – Т. X, № Y (или: Вып. Z). – С. XXX–YYY. – DOI: xxx.

Пример:
Иванов И.И., Петрова П.П. Искусственный интеллект в образовании // Вестник БГУ. – 2024. – № 3. – С. 45–56. – DOI: 10.1234/abc.

ГОСТ Р 7.0.100–2018 - Монография
Фамилия И.О., Фамилия И.О. Название книги. Город: Издательство, Год. Кол-во страниц.

Пример:
Сидоров В.В. Основы машинного обучения. Москва: Издательство МГУ, 2023. 350 с.

ГОСТ Р 7.0.100–2018 - Диссертация
Фамилия И.О. Название диссертации: дис. ... канд./докт. наук: спец. ХХХХ.ХХ.ХХ. Место защиты, Год.

Пример:
Иванов И.И. Методы оптимизации нейросетей в образовательных системах: дис. ... канд. физ.-мат. наук: 01.01.09. Минск, 2024.

ГОСТ Р 7.0.100–2018 - Электронный ресурс
Фамилия И.О. Название [Электронный ресурс] // Название сайта. – Дата публикации. – URL: https://.... (дата обращения: ...).

Пример:
Лебедев А.А. Будущее искусственного интеллекта [Электронный ресурс] // Medium. – 2024. – URL: https://medium.com/... (дата обращения: 22.01.2026).

ВАК (Высшая аттестационная комиссия) требования:
✓ Все авторы русских статей должны быть указаны с инициалами
✓ Использование DOI обязательно для публикаций после 2016 года
✓ Минимум 15 источников для диссертации (не менее 70% на русском)
```

### B. Тестовые данные (примеры запросов)

```json
{
  "test_queries": [
    {
      "id": "test_1",
      "query": "нейросети в образовании",
      "expected_min_results": 10,
      "expected_concepts": ["education", "neural-networks"]
    },
    {
      "id": "test_2",
      "query": "machine learning healthcare",
      "expected_language": ["en"],
      "expected_min_results": 5
    }
  ],
  "test_bibliography": [
    {
      "id": "bib_test_1",
      "articles": ["openalex_w123", "cyberleninka_abc"],
      "expected_gost_format": "1. Иванов И.И..."
    }
  ]
}
```

---

## 🎉 ГОТОВО К РАЗРАБОТКЕ!

**Этот документ полностью описывает MVP и готов к передаче в разработку.**

**Следующие шаги:**
1. ✅ Скопируйте ТЗ в свою систему управления проектами (Notion, Jira)
2. ✅ Созовите встречу с техническую командой
3. ✅ Создайте GitHub репозиторий
4. ✅ Регистрируйте аккаунты на OpenAlex, Claude, Yandex Cloud
5. ✅ Начните спринт 1 (недели 1-2): Setup + архитектура

---

**Версия:** 1.0  
**Дата последнего обновления:** 10 февраля 2026  
**Статус:** Утвержден для разработки MVP